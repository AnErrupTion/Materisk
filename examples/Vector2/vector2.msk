import "stdlib.msk";

mod pub dyn Vector2
{
	fld pub x int
	fld pub y int

	fn pub ctor(int x, int y) void
	{
		self.x = x;
		self.y = y;
	}

	fn pub print() void
	{
		Console.print(string "<Vector2 x=");
		Console.print(string Int.toString(self.x));
		Console.print(string " y=");
		Console.print(string Int.toString(self.y));
		Console.println(string ">");
	}

	fn pub add(Vector2 other) Vector2
	{
		return new Vector2(self.x + other.x, self.y + other.y);
	}
}